# Arsitektur & Flow Diagram

## 1. Layer Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    UI LAYER                                 │
│  (Screens: MyItineraryScreen, ItineraryScreen, dll)         │
│  - ConsumerStatefulWidget / ConsumerWidget                  │
│  - UI Components sama User Interactions                     │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   │ ref.watch() / ref.read()
                   ▼
┌─────────────────────────────────────────────────────────────┐
│                    STATE MANAGEMENT                         │
│  (Riverpod Providers & Notifiers)                           │
│  - activitiesProvider (StateNotifier)                       │
│  - activitiesByCategoryProvider (StateNotifier)             │
│  - categoriesProvider                                       │
│  - isLoadingProvider                                        │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   │ operasi delegasi
                   ▼
┌─────────────────────────────────────────────────────────────┐
│                    SERVICE LAYER                            │
│  (HiveService)                                              │
│  - addActivity(activity)                                    │
│  - updateActivity(index, activity)                          │
│  - deleteActivity(index)                                    │
│  - getActivities()                                          │
│  - getActivitiesByCategory(category)                        │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   │ operasi CRUD
                   ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATA LAYER                               │
│  (Hive Database)                                            │
│  - Hive Box<ActivityModel>                                  │
│  - Local Storage (Persisten)                                │
│  - Auto-serialization via Adapters                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. Flow Add Activity

```
┌─────────────────────────────────────────────────────────────┐
│  USER klik tombol "TAMBAH WISATA"                           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  MUNCUL DIALOG FORM                                         │
│  - field Name                                               │
│  - field Location                                           │
│  - field Notes                                              │
│  - Date picker                                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  USER isi form terus klik "SIMPAN"                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  BIKIN ActivityModel                                        │
│  {                                                          │
│    name: "...",                                             │
│    location: "...",                                         │
│    notes: "...",                                            │
│    category: selectedCategory,                              │
│    date: selectedDate                                       │
│  }                                                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  ref.read(activitiesProvider.notifier).addActivity(activity)│
│  (Call Riverpod StateNotifier)                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  ActivitiesNotifier.addActivity()                           │
│  - Call HiveService.addActivity(activity)                   │
│  - Call _loadActivities() to refresh state                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  HiveService.addActivity()                                  │
│  - _box.add(activity)  ← Save to Hive                       │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  HIVE DATABASE                                              │
│  ✅ Data saved to local storage (persistent!)               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  _loadActivities() UPDATES STATE                            │
│  state = _hiveService.getActivities()                       │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  RIVERPOD NOTIFIES ALL WATCHERS                             │
│  ref.watch(activitiesProvider) listeners updated           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  UI AUTOMATICALLY REBUILDS                                  │
│  - New activity appears in list instantly                   │
│  - Dialog closes                                            │
│  - No setState() needed!                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Data Flow - App Restart (Persistence)

```
┌─────────────────────────────────────────────────────────────┐
│  USER CLOSES & REOPENS APP                                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  main() CALLED                                              │
│  - WidgetsFlutterBinding.ensureInitialized()                │
│  - HiveService.initialize()                                 │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  HiveService.initialize()                                   │
│  - Hive.initFlutter()                                       │
│  - Hive.registerAdapter(ActivityModelAdapter())             │
│  - _box = Hive.openBox<ActivityModel>('activities')         │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  HIVE LOADS DATA FROM DISK                                  │
│  ✅ All previous data accessible from _box                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  User navigates to MyItineraryScreen                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  build(context, ref) CALLED                                 │
│  final activities = ref.watch(activitiesProvider)           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  activitiesProvider LOADS INITIAL STATE                     │
│  - ActivitiesNotifier constructor called                    │
│  - _loadActivities() executed                               │
│  - state = _hiveService.getActivities()                     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  HiveService.getActivities()                                │
│  return _box.values.toList()  ← Read from disk              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  UI SHOWS ALL PERSISTED ACTIVITIES                          │
│  ✅ "Test Attraction" and other data are back!              │
│  ✅ User sees: "It works! 🎉"                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. State Flow Diagram

```
           ┌──────────────────────┐
           │  ConsumerWidget      │
           │  build(ref)          │
           └──────────┬───────────┘
                      │
                      │ ref.watch()
                      ▼
           ┌──────────────────────────────────┐
           │  activitiesProvider              │
           │  StateNotifier<List<Activity>>   │
           │                                  │
           │  state = [activity1, activity2]  │
           └──┬──────────┬──────────┬────────┘
              │          │          │
           add()      edit()     delete()
              │          │          │
              └──────┬───┴──────┬───┘
                     │          │
                     ▼          ▼
           ┌─────────────────────────────┐
           │  HiveService               │
           │  - addActivity()            │
           │  - updateActivity()         │
           │  - deleteActivity()         │
           └─────────────┬───────────────┘
                         │
                         ▼
           ┌─────────────────────────────┐
           │  Hive Box                   │
           │  [ActivityModel, ...]       │
           │  (Local Storage)            │
           └─────────────────────────────┘
                         │
           ┌─────────────┘
           │
           └──> _loadActivities()
                  │
                  ▼
           ┌──────────────────────────┐
           │  state updated           │
           │  Notifies all watchers   │
           └──────────────┬───────────┘
                          │
                          ▼
           ┌──────────────────────────┐
           │  UI Rebuilds             │
           │  ListView updates        │
           │  Data visible to user    │
           └──────────────────────────┘
```

---

## 5. Provider Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                    PROVIDER ECOSYSTEM                       │
└─────────────────────────────────────────────────────────────┘

                    hiveServiceProvider
                           │
                           │ (singleton instance)
                           ▼
    ┌──────────────────────────────────────────────┐
    │                                              │
    │  activitiesProvider                         │
    │  (Manages ALL activities from Hive)         │
    │  StateNotifier<List<ActivityModel>>         │
    │                                              │
    └──────────────────────────────────────────────┘
         │                              │
         │                              │
    ┌────▼────────────────────────────────┴────┐
    │                                           │
    ▼                                           ▼
    
activitiesByCategoryProvider            activitiesByCategoryProvider
    (Sightseeing)                         (Restaurant)
  -> Filters activities by             -> Filters activities by
     Sightseeing category                 Restaurant category


    categoriesProvider
    -> Returns static list of 6 categories
       [Sightseeing, Restaurant, Nightlife, Hotel, Shopping, Cinema]


    isLoadingProvider
    -> Boolean flag for loading state
```

---

## 6. Sequence Diagram - Edit Activity

```
User              UI                 Provider          HiveService        Hive DB
 │                │                     │                  │               │
 │   Tap Edit     │                     │                  │               │
 ├───────────────>│                     │                  │               │
 │                │                     │                  │               │
 │                │ Show Edit Dialog    │                  │               │
 ├<───────────────┤                     │                  │               │
 │                │                     │                  │               │
 │ Fill Form      │                     │                  │               │
 ├───────────────>│                     │                  │               │
 │                │                     │                  │               │
 │ Tap Simpan     │                     │                  │               │
 ├───────────────>│                     │                  │               │
 │                │                     │                  │               │
 │                │ ref.read().update() │                  │               │
 │                ├────────────────────>│                  │               │
 │                │                     │                  │               │
 │                │                     │ updateActivity() │               │
 │                │                     ├─────────────────>│               │
 │                │                     │                  │               │
 │                │                     │                  │ _box.putAt()   │
 │                │                     │                  ├──────────────>│
 │                │                     │                  │               │
 │                │                     │                  │  ✅ Updated   │
 │                │                     │                  │<──────────────┤
 │                │                     │                  │               │
 │                │                     │ _loadActivities()│               │
 │                │                     ├─────────────────>│               │
 │                │                     │                  │               │
 │                │                     │ state updated    │               │
 │                │<────────────────────┤                  │               │
 │                │                     │                  │               │
 │ ✅ List Updates│ (auto rebuild)       │                  │               │
 │<───────────────┤                     │                  │               │
 │                │                     │                  │               │
```

---

## 7. Error Handling Flow

```
┌─────────────────────────────────────┐
│  Operation Called                   │
│  (add, update, delete)              │
└────────────────┬────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │  Try Block     │
        └────────┬───────┘
                 │
         ┌───────┴────────┐
         │                │
         ▼                ▼
    ✅ SUCCESS        ❌ EXCEPTION
         │                │
         ▼                ▼
    _loadActivities()   catch(e)
    state updated        print(e)
    UI updates          state unchanged
                        Graceful fallback
```

---

## 8. Before vs After Architecture

### BEFORE: Using ActivityStore
```
Screen
  │
  └─> ActivityStore (Singleton)
        │
        └─> Memory Only
              │
              └─> Lost on restart ❌
```

### AFTER: Using Riverpod + Hive
```
Screen (ConsumerWidget)
  │
  └─> Riverpod Provider (ref.watch/read)
        │
        ├─> StateNotifier (Business Logic)
        │     │
        │     └─> HiveService (Database Operations)
        │           │
        │           └─> Hive Box (Persistent Storage)
        │                 │
        │                 └─> Survives app restart ✅
        │
        └─> Reactive updates (auto rebuild)
              │
              └─> No setState() needed ✅
```

---

## 9. Data Structure Example

```
Hive Box: 'activities'
│
├── Index 0
│   ├── name: "Golden Pavilion"
│   ├── location: "Kyoto, Japan"
│   ├── notes: "Kuil Budha yang indah..."
│   ├── category: "Sightseeing"
│   ├── date: DateTime(2026, 3, 15)
│   └── imagePath: "assets/images/..."
│
├── Index 1
│   ├── name: "Ichiran Ramen"
│   ├── location: "Kyoto Station"
│   ├── notes: "Ramen tonkotsu terkenal..."
│   ├── category: "Restaurant"
│   ├── date: DateTime(2026, 3, 15)
│   └── imagePath: "assets/images/..."
│
├── Index 2
│   ├── name: "Pontocho Alley"
│   ├── location: "Kyoto, Japan"
│   ├── notes: "Bar dan teahouse tradisional..."
│   ├── category: "Nightlife"
│   ├── date: DateTime(2026, 3, 17)
│   └── imagePath: "assets/images/..."
│
└── ... more items
```

---

## 10. Testing Flow

```
APP STARTUP
    │
    ▼
Add Activity "Test"
    │
    ▼
✅ See in UI immediately
    │
    ▼
Close App
    │
    ▼
Reopen App
    │
    ▼
Navigate to Wisata Screen
    │
    ▼
✅ "Test" activity still there!
    │
    ▼
PERSISTENCE VERIFIED ✅
```

---

This architecture ensures:
- ✅ **Clean Separation**: UI, State, Business Logic, Data layers
- ✅ **Reactive**: Auto-updates when state changes
- ✅ **Persistent**: Data survives app restarts
- ✅ **Scalable**: Easy to add more providers
- ✅ **Testable**: Each layer can be tested independently
